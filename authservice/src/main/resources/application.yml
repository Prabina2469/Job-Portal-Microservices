server:
  port: 8081


spring:
  application:
    name: auth-service


  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: JHEX@2025


  jpa:
    hibernate:
      ddl-auto: update


  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      properties:
        max.block.ms: 3000
        request.timeout.ms: 3000
        delivery.timeout.ms: 5000


  # ? ADD THIS ? so auth-service can send OTP emails directly
  mail:
    host: smtp.gmail.com
    port: 587
    username: rout35803@gmail.com
    password: uoykvcmiopnfzgvn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka